# Zoolanding Data Dropper Lambda Codex Memory

## Current Decisions

- 2026-07-15 05:07 CT deployment identity: `dev` is local-only and has no AWS deployment profile, workflow, Environment, or dedicated cloud resources. Protect `dev`, `test`, and `main`; promote only `feature -> dev -> test -> main` from the same repository. Test and production must set both AWS region variables explicitly, build and test without OIDC, transfer a manifest-verified same-run artifact, prove both merged-PR parents and refetch the current target tip, use non-cancelling concurrency, and keep the OIDC job free of checkout or repository-code execution before deploying only the verified artifact.
- 2026-05-21 15:23 CT presentation wording cleanup: for `talk-python-cdmx-analytics/slides-aws-ajolotes.md`, avoid ambiguous phrases such as "fronteras AWS", "boundary de compute", "Lambda delgada", and mixed raw/analytics wording in visible speaker-facing copy. Prefer "responsabilidad clara por servicio", "entrada publica controlada", "codigo bajo demanda", "evento original", and "datos sin transformar". The map footer should stay short enough to fit: "Regla: Lambda valida; S3 conserva el evento original."
- 2026-05-21 14:55 CT presentation naming correction: "AWS: Ajolotes en la Nube" is the AWS Ciudad de Mexico community that invited the talk, not a technical metaphor. The deck should not include a "why ajolotes" or adaptability-explanation slide; keep the community mention as invitation/context on the cover and speaker guide only.
- 2026-05-21 14:50 CT presentation refinement: `slides-aws-ajolotes.md` should be self-contained slide-by-slide. Avoid visible language that depends on the prior Clara deck, previous/next slides, or numbered decision sequences. Use standalone claim titles and redacted SVG visuals for CloudFront, API Gateway, Lambda env vars, and DevTools instead of raw console screenshots that may expose operational identifiers.
- 2026-05-21 14:34 CT presentation update: keep `talk-python-cdmx-analytics/slides-clara.md` as the Clara/source variant and use `talk-python-cdmx-analytics/slides-aws-ajolotes.md` for the new AWS-first conference, "AWS: Ajolotes en la Nube". The new deck centers responsibilities by service: CloudFront/API Gateway as the controlled public entry, Lambda Python as code that validates and names the event, S3 as storage for the original event, IAM minimum privilege, SAM IaC, logs/costs/security, and ETL/Athena/Glue as future layers. Generated PPTX lives in `C:\Users\lince\.codex\memories\Output\AWS-Ajolotes-en-la-Nube.pptx`; visual previews live under `C:\Users\lince\.codex\memories\Output\zoolanding-data-dropper-lambda\aws-ajolotes-preview.*`. During live demos, avoid showing account IDs, API IDs, CloudFront distribution IDs, cookies, secrets, or personal data.
- 2026-05-13 18:48 CT timezone closeout: keep the raw analytics body unchanged and keep the S3 key partition in UTC as `appName/YYYY/MM/DD/<timestampMs>-<shortRequestId>.json`. When a payload includes a valid IANA `timezone`, the Lambda computes viewer-local time with Python `zoneinfo` and exposes it in the response, logs, and S3 object metadata. If `timezone` is missing or invalid, accept the event with UTC-only time fields; do not infer local time from the Lambda region, IP, or incomplete headers.
- 2026-05-13 18:59 CT deployment closeout: `sam deploy` created or updated the `zoolanding-data-dropper` stack in `us-east-1`. The public API CloudFront distribution needed an explicit `/analytics` behavior pointed at the SAM API origin; after the CloudFront deployment completed, synthetic requests from both `https://test.zoolandingpage.com.mx` and `https://zoolandingpage.com.mx` returned `eventTime` and wrote S3 object metadata. Avoid committing raw distribution IDs, API IDs, account IDs, or temporary audit object keys in docs.
- 2026-05-13 19:14 CT ETL handoff decision: do not change Zoolandingpage just to repeat `timezone` on every analytics event. Future ETL should connect events by `sessionId`, choose a session timezone from the first valid IANA `timezone` in the session, and apply it to later events that omit timezone while preserving raw payloads and UTC partitions.
- 2026-06-29 07:16 CT blog analytics privacy hardening: blog/content-hub events are still stored as the original raw body, but they are rejected before storage when any nested field or string value contains obvious personal or credential material. Successful public responses no longer include the raw S3 bucket name or object key.
- 2026-06-29 21:19 CT blog analytics runtime shape: blog/content-hub events may provide safe `contentHubId`/`hubId` and `articleId`/`slug` either at the top level or inside `meta`/`metadata`. This supports Zoolanding runtime/component analytics while keeping the original body unchanged and preserving recursive PII/credential rejection.

## Verification Commands

- `python -m unittest discover -s tests`
- `$env:DRY_RUN = "1"; python .\local_test.py`

## Security Notes

- Timezone is useful context but can still be sensitive. Do not add IP, precise geolocation, cookies, or other sensitive analytics enrichment without the configured consent flow and compliance review.
